{
  "name": "narwallet-extension",
  "version": "4.0.7",
  "description": "Narwallet chrome-extension wallet for Utility blockchain",
  "author": "Lucio Tato <lucio@metapool.app>",
  "homepage": "www.narwallets.com",
  "type": "module",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Narwallets/narwallets-extension.git"
  },
  "scripts": {
    "build": "tsc -b -verbose tsconfig-sourcemap.json && rm -rf test-new-install-ln && ln -sf extension test-new-install-ln",
    "start": "git pull && npm run build && echo done. open the extension form the browser",
    "test": "chrome chrome://extensions/"
  },
  "keywords": [
    "Utility",
    "Narwallet"
  ],
  "devDependencies": {
    "@types/chrome": "^0.0.197"
  },
  "dependencies": {
    "web3-api-js": "^1.3.5"
  }
}